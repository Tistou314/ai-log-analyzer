# Moteur : c'est tout ce qu'il faut pour `python cli.py access.log`
pandas>=2.0
numpy>=1.24
openpyxl>=3.1  # exports Search Console en xlsx (--gsc, --gsc-ai, --crawl-stats)
# Optionnel, intégration LLM (--diagnose, chat) : sans clé, les prompts/ se copient à la main
anthropic>=1.0
openai>=1.50
